apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  selector:
    app: myapp
  ports:
  - protocol: TCP
    port: 80                # Service port
    targetPort: 80          # Container port (match containerPort above)
    nodePort: 30007         # External access port (30000–32767)
